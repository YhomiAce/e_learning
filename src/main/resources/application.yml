spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/sp_lesson_four
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    show-sql: true